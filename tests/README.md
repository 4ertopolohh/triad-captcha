# Repository integration tests

Package-level tests live beside each distributable package. This directory holds
cross-package and deployment-contract checks that guard invariants such as the
private Redis network, Nginx directive contexts, redacted edge logging, and secure
dependency floors.

